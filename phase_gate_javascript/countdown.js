/*Create a while loop that starts from 50 and countdown to 0 decrementing by 5 each iteration. print each number in js*/
let start = 50;
let end = 0;
while(start >= end) {
    console.log(start);
	start -= 5;
}
